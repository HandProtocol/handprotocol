[project]
name = "yuhm-matching-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "ortools>=9.12,<10",
  "psycopg[binary]>=3.2,<4",
]

[project.scripts]
yuhm-matching-worker = "yuhm_food_worker.main:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
