# Dependencies
node_modules/
.pnp.*

# Build output
.next/
out/
dist/
build/

# Environment
.env
.env.local
.env.*.local
sample.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Solidity
.deps/
.deploys/
artifacts/
cache/

# Blender autosave backups (keep .blend + .py + committed renders)
*.blend1
*.blend2

# Misc
.bolt/

# Local Netlify folder
.netlify
.gstack/

# Personal property notes (kept local, not org-repo material)
# Anchored to repo root so the deployed web surface web/mosquitos/ stays tracked.
/mosquitos/

# NotebookLM CLI state (local artifacts, references personal notebook IDs)
funding/updates/.history.jsonl
funding/updates/.notebooks.json

# Audio renders (too large for git history; keep in cloud storage / NotebookLM)
funding/updates/*.m4a
__pycache__/

# Lead source photos (originals); only the optimized webp under web/demos/ are committed
biz/*/photos/

# Claude Code local settings (machine-specific)
.claude/settings.local.json
